import { createContext } from "react";
export const recipeNameContext = createContext("");

export const recipeIdContext = createContext("");

// export const recipeIngredientContext = createContext([]);
export const recipeCalorieContext = createContext(0);

export const recipeMealTimeContext = createContext("");
